import { StateDefining } from './db';
import { DBAction } from './actions';
export declare function reduce<State extends StateDefining>(state: State, action: DBAction, options: {
    initialState: State;
}): State;
export declare function reducer<State extends StateDefining>(initialState: State): (state: State | null | undefined, action: DBAction) => State;
